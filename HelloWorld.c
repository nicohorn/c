// Preprocessor command that tells the compiler to include the contents of a file.
#include <stdio.h>

//The entry point of the program is the main function. Everything in the main function is read procedurally, from top to bottom.
int main(){


    //Printing to console.
    printf("Hello World\n");
    printf("I like pizza, it's really good");

    //We return a 0 if our program runs succesfully, without errors. If there's an error, we'll return a 1.
    return 0;
}

