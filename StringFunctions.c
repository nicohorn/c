#include <stdio.h>


int main(){

    char string1[] = "Nico";
    char string2[] = "Horn";


    //strlwr(string1);                    // converts a string to lowercase
    //strupr(string1);                    // converts a string to uppercase
    //strcat(string1, string2);           // appends string 2 to end of string 1
    //strncat(string1, string2, 1);       // appends n characters from string 2 to string 1
    //strcpy(string1, string2);           // copy string 2 to string 1
    //strncpy(string1, string2, 3);       // copy n characters of string 2 to string 1

    //strset(string1, '?');               // sets all the characters of a string to a given character
    //strnset(string1, 'x', 1);           // sets first n characters of a string to a given character
    //strrev(string1);                    // reverse a string;          

    //int result = strlen(string1);                 // returns string length as an int
    //int result = strcmp(string1, string2);        // string compare all characters
    //int result = strncmp(string1, string2, 1);
    //int result = strcmpi(string1, string2);       //Same as strcmp but ignore case
    //int result = strncmpi(string1, string2);      //Same as strncmp but ignore case


    printf("%s, %d", string1, result);



    return 0;   
}